# Playback recovery — 2026-09-26 11:46

The listener praised the opened-up corpus mix and asked to preserve it. The
composition and mixer were already committed/pushed at `c3a6b36`.

## Observed failure

- scsynth was absent from the process table and UDP 57110 was absent.
- Plugin-owned sclang (711899, under wrapper 711896) and GHCi (711897) survived;
  both roots were direct children of the current Pi process 635550.
- `tidal_status` and `tidal_sc` recovery hit the stale runtime helper error:
  `(0 , _processTree.stopOwnedProcessTree) is not a function`.
- No intentional hush/stop preceded the silence. Cause of scsynth's exit remains
  unknown; the bounded journal query did not establish a crash/OOM cause.
- Plugin recording status was stale: it still claimed recording after the audio
  server had gone. Do not equate that status with captured audio.

## Preservation and recovery

1. Stopped the recording through `tidal_record`. Verified the actual FLAC exists:
   `recordings/jam-20260926-1117.flac`, 120,561,709 bytes, **1356.458667 seconds**.
   WAV and markers are retained locally. Wall-clock recording state had reported
   1620 seconds, longer than the actual captured audio.
2. Re-verified parent PID and command signatures for the two stranded plugin
   roots. Used the current on-disk `stopOwnedProcessTree` helper, including its
   start-time checks, to stop only those owned trees. No executable-name kills.
3. Called `tidal_status`; the plugin successfully booted its managed stack and
   REPL once the stranded children were gone. No manual competing SC stack.
4. Recalled `sc/samples70.scd` and `sc/open70.scd`; sent `resetCycles`, then
   `:script /home/erik/livecode/70.tidal`.
5. Started new recording `recordings/jam-20260926-1146.wav`.
6. Confirmed 65 incoming Dirt events over roughly 3 seconds and nonzero stereo
   hardware-output-bus amplitude (observed envelopes up to 0.0464 / 0.0385).

No musical/mixer changes were introduced during this recovery. The opened-up
mix remains at master cutoff 18k, saturation 0.1, glue slope 0.8, threshold 0.5,
master duck 0.04, gain 0.8 and limiter ceiling 0.79. Dry drums have explicit 18k
cutoff and no delay/reverb sends.

## Remaining caveat

The running extension still predates the content-versioned helper import fix
already committed in pi-tidal (`1811eb3`). A controlled Pi reload is still needed
to activate that lifecycle fix permanently. The recovery above bypassed the
stale helper only for verified stranded-process cleanup; it did not hot-patch the
loaded extension. Keep this separate from composition and filter/mix diagnosis.

There is a genuine common master low-pass in the DSP code; orbit send effects
and group aux returns precede the master. That explains why a master filter move
can darken everything, but does not prove an accidental feedback/routing bug.
